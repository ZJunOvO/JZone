# JZone 动效与液态玻璃 Tasks（2026-07-11）

关联 PRD：`docs/ANIMATION_AND_LIQUID_GLASS_PRD.md`

## Task 1：性能与材质地基

- 状态：进行中
- [x] 将四个核心页面、播放页和合集详情改为路由级懒加载。
- [x] 将入口包由约 503.9 KB 降至约 260.5 KB。
- [x] 移除 Mini 播放器和 Mini 菜单祖先上的 `filter: blur(0)` / 入场 filter，恢复背景采样。
- [x] Mini 播放器重建为单层 F 材质，并以独立兄弟层承载缩放模糊动画。
- [x] Mini 菜单复用单层 F 材质。
- [x] 建立 `prefers-reduced-motion` 降级。
- [ ] 在安卓 Edge 和鸿蒙兼容层实测材质降级、首屏时间与持续滚动帧率。

## Task 2：资料库 F 材质与 Bento 稳定性

- 状态：进行中
- [x] 删除已放弃的液滴拖拽组件、手势和文档要求。
- [x] 资料库“新建 + 视图切换”分别使用原始 Shuding F 材质。
- [x] Mini 播放器迁移到同一 `LiquidGlassSurface`，移除旧毛玻璃材质。
- [x] Shuding 轮廓与底部 Tab 统一读取高级设置及边缘高光参数。
- [x] 建立不依赖 `mix-blend-mode` 的全局液态玻璃图标亮度采样规则。
- [x] 底部 Tab 激活图标固定为主题红；创作加号保持空心。
- [x] Mini 菜单移除全屏模糊，改为菜单内容自身模糊淡入/淡出。
- [x] Mini 播放器和 Mini 菜单启用宽面板全幅中心折射，避免只有边缘高光。
- [x] Mini 播放器移除变换祖先，并将 Tab 间距压缩到经三视口压力测试通过的 14px。
- [x] SVG 位移图补齐 `xlink:href`，移动设备增加毛玻璃兜底。
- [x] Bento 固定网格坐标与拖拽 transform 解耦，不再吸附左上角。
- [x] 拖动时高亮最近放置目标，交换顺序写入当前布局缓存。
- [x] 双指缩放和单指移动超过阈值时取消记忆卡片长按。
- [x] Bento 画布扩展到完整动态视口，底部 Tab 与 Mini 播放器浮于内容上方。
- [x] Bento 标签改为居中靠上的纯文字，排序入口移动到视图切换右侧。
- [x] 首帧同步读取缓存布局并延迟启用 layout projection，消除往返视图的假动画。
- [ ] 安卓 Edge 与鸿蒙设备实测右上控件折射效果。

## Task 3：共享元素第一批

- 状态：待开始
- [ ] 现在就听头像到个人页头像，支持前进和返回。
- [ ] Mini 播放器到全屏播放页，封面与标题保持对象连续性。
- [ ] 歌曲封面到全屏播放页封面。
- [ ] 歌单 / 专辑封面到合集详情页。
- [ ] 处理目标离屏、图片未加载和减少动态模式的降级。

## Task 4：导航与布局转换

- 状态：待开始
- [ ] 四核心页面统一低位移淡入淡出参数。
- [ ] 资料库列表 / Bento 切换建立容器过渡，不修改 Bento 内部设计。
- [ ] Mini 菜单切换锚点时平移和尺寸自适应，不重复完整入场。
- [ ] 快速连续切页可中断，不产生历史页面闪回。

## Task 5：操作反馈

- 状态：待开始
- [ ] 收藏、公开/私有、置顶使用原位状态反馈。
- [ ] 加入歌单 / 专辑使用骨架列表项与真实内容交接。
- [ ] 上传队列使用稳定占位，保存成功后进入下一项。
- [ ] 删除项在远程成功后收拢，失败恢复。
- [ ] 菜单统一“模糊建立 → 形变展开 → 内容淡入”节奏。

## Task 6：播放沉浸动效

- 状态：待开始
- [ ] 播放 / 暂停图标形态转换。
- [ ] Mini 播放器与全屏播放页状态同步过渡。
- [ ] 拖拽进度条实时跟手，松手提交 seek。
- [ ] 封面、背景色和播放状态变化使用统一节奏。
- [ ] 歌词动画另立 PRD，不并入本任务。

## Task 7：严格回归与性能验收

- 状态：待开始
- [ ] `npm run check`、构建、登录 smoke、合集 smoke 全部通过。
- [ ] 桌面和移动宽度完成截图对比，检查遮挡、文字溢出和安全区。
- [ ] 记录长按拖拽、共享转场和菜单切换期间的长任务。
- [ ] 检查玻璃控件不存在历史材质叠加，移动端无 SVG 支持时不退化为纯透明。
- [ ] 安卓 Edge、鸿蒙兼容层、iOS Safari 分别记录原生效果或降级结果。
- [ ] 回看五批任务，修复后再更新项目地图和备忘录。
