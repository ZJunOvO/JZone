# JZone 手机实测反馈优化 Tasks（2026-07-11）

## Task 1：播放计数口径

- 状态：已完成
- [x] 从固定 30 秒改为有效播放区间的 20%。
- [x] 裁剪歌曲使用 `trim_end - trim_start`，未裁剪歌曲使用歌曲时长。
- [x] 保留单会话只计一次及 seek 防刷逻辑。
- 验证：240 秒歌曲播放 35 秒不计数，播放超过 48 秒后增加一次。

## Task 2：当前用户身份与头像

- 状态：已完成
- [x] 当前艺人等待 profile 解析，不再先以 QQ 邮箱前缀抢占主艺人。
- [x] 使用 profile UUID 归一化主艺人并移除重复合作角色。
- [x] profile、头像签名 URL 使用长期缓存并每 30 分钟后台刷新。
- [x] COS 完整旧签名 URL 重新提取对象 Key 后签名。
- 验证：首次上传竞态下最终为“纸菌live / 主艺人”，无 QQ 重复角色；首页头像加载尺寸正常。

## Task 3：创作页统计

- 状态：已完成
- [x] 移除草稿、任务两个弱统计项。
- [x] 恢复私有与空间，并将第一项调整为总播放量。

## Task 4：现在就听布局与缓存

- 状态：已完成
- [x] 继续播放与常听改成横向吸附轨道，右侧露出常听入口。
- [x] 常听、继续播放和头像使用长期缓存，进入页面先显示旧数据。
- [x] 个人播放数据每 30 分钟后台刷新，最新公开随歌曲数据每 5 分钟刷新。
- 验证：页面往返后继续播放、常听、头像立即存在，未重复请求个人播放统计。

## Task 5：待播放与集合排序

- 状态：已完成
- [x] 文案统一为“待播放”。
- [x] 正常模式不挂载拖拽项；进入排序模式后才显示拖拽把手。
- [x] 待播放只有拖拽把手触发排序，其他区域可滚动。
- [x] 集合详情增加显式排序模式并持久化 `sort_order`。
- [x] 新增远程 RPC `reorder_collection_songs`，仅授权 authenticated 且复用协作者权限判断。
- 验证：歌单顺序远程交换成功并已恢复原顺序；匿名角色无 RPC 执行权限。

## Task 6：封面上传稳定性

- 状态：已完成
- [x] 集合、歌曲和上传草稿封面统一预缩放。
- [x] 移除集合封面的 Base64 `FileReader` 链路，改用 Blob URL。
- [x] 进入裁剪器前限制最大 2048px，Canvas 增加零尺寸保护。
- 验证：歌单编辑页完成选择与裁剪，主页面无导航、无刷新、无 Canvas 错误。

## Task 7：安卓无后缀录音

- 状态：已完成
- [x] 不再静默过滤文件选择器返回的 0 字节占位文件。
- [x] 增加读取中状态和零字节重试，失败时显示明确反馈。
- [x] `application/octet-stream` 不再覆盖魔数识别结果。
- [x] 文件选择器补充 generic MIME、3GP 和常用音频扩展名。
- 验证：使用真实 M4A 内容模拟无后缀、generic MIME 文件，成功识别为 M4A 并进入编辑流程。
