我将分步为您实现全站内容管理闭环，并完成从 Mock 数据到真实 Supabase 后端的迁移。

### 📅 实施计划

#### Phase 1: 基础设施恢复与数据库变更
1.  **依赖安装**：安装 `@supabase/supabase-js`，确保前端能连接后端。
2.  **客户端配置**：创建 `lib/supabase.ts`，恢复 Supabase 连接配置。
3.  **Schema 变更**：创建 `supabase/sql/009_content_management.sql`，执行数据库字段扩展（`is_public`, `pinned_at`）。
4.  **类型定义**：更新 `types.ts`，添加新字段定义。

#### Phase 2: 核心组件开发 (`UniversalContextMenu`)
1.  **组件实现**：创建 `components/UniversalContextMenu.tsx`，实现基于 `framer-motion` 的长按/点击菜单。
2.  **权限控制**：实现 `Owner` vs `Viewer` 的菜单项过滤逻辑。
3.  **交互逻辑**：实现置顶、可见性切换、删除的 API 调用占位（将在 Store 改造中实装）。

#### Phase 3: 数据层改造 (Store Refactor)
1.  **Store 升级**：改造 `store.tsx`，将 `useState(MOCK_SONGS)` 替换为 `useEffect` 从 Supabase `songs` 表实时拉取数据。
2.  **API 实现**：在 Store 中实现 `toggleVisibility`, `togglePin`, `updateSong`, `deleteSong` 等 CRUD 方法。
3.  **Mock 清理**：确认 Store 不再依赖 `constants.ts` 后，**删除该文件**。

#### Phase 4: UI 完善
1.  **编辑模态框**：实现 `components/EditSongModal.tsx`。
2.  **上传页优化**：在 `pages/Upload.tsx` 中添加“发布到广场”开关。
3.  **接入菜单**：在 `Library` 和 `PlayerView` 中接入 `UniversalContextMenu`。

我现在开始执行 **Phase 1** 和 **Phase 2**（优先输出您要求的 SQL 和 菜单代码）。
